责任链模式（Chain of Responsibility Pattern）：顾名思义，就是为请求创建一个接收者对象的链。在这种模式中，每个请求接收者都包含对另一个
请求接收者的引用，如果一个接收者对象不能处理请求，就将请求传递给下一个接收者
优点：1.降低耦合，它将请求的发送者和接收者解耦2.增强给对象指派职责的灵活性，允许动态增加和删除接收者，增加新的请求处理类很方便
缺点：涉及到很多的接收者调用，可能性能会较低

应用实例：Spring MVC的过滤器