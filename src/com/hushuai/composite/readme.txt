组合模式：composite pattern
又叫“部分-整体”模式，组合模式依据树形结构来组合对象，用来表示部分及整体层次。属于结构性模式，它创建了对象组的树形结构
如果多个类型结构类似，都是整体包含多个相同子部分，并且最低层级的子部分不再包含更低层次的子分部，类似于树形结构。例如操作系统的文件系统，
父文件夹包含子文件夹，子文件夹又包含子子文件夹和文件，文件则没有了子部分
组合模式就是用来处理树形结构的数据的

举例：
一本书的目录结构，按照层次结构分别包含书名-章节-文章